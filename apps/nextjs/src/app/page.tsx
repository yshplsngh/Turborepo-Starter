import { log } from '@repo/logger'; 
import {Button} from "@repo/ui/button"
export default function Page() {
  log('papa');
  return (
    <main>
      <h1 className="text-red-600">Turbo repo setup</h1>
    </main>
  );
}
